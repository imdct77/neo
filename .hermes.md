# {PROJECT_NAME} — 최우선 절대 규칙
# 파일 위치: 프로젝트 루트 .hermes.md
# 컨텍스트 압축 후에도 가장 마지막까지 살아남아야 할 규칙만 담는다.
#
# ⚠️ 작성 지침:
#   이 파일에는 이 프로젝트에서 "이유가 있어도 절대 하면 안 되는 것"만 작성한다.
#   - 비즈니스 로직의 핵심 불변 조건
#   - 한 번 위반하면 큰 피해가 발생하는 것
#   - LLM이 "이 경우는 예외겠지"라고 합리화할 수 있는 것
#   좋은 Omission Constraint는 "무엇을 금지하는가"와 "왜 금지인가"를 함께 명시한다.

## Project Identity

PROJECT_ID: {PROJECT_ID}
PROJECT_NAME: {PROJECT_NAME}

# PROJECT_ID는 setup.py가 자동 생성한다 (kebab-case 소문자).
# 예) my-recipe-app, auth-service, jgbg-platform
# 재정의 금지: 최초 할당 후 변경하지 않는다.
# 이유: 이미 저장된 모든 mem0 기록의 접두사가 깨지기 때문이다.

## 세션 시작 (최우선)

```
python3 harness/harness-env.py
```
→ `HARNESS_ROOT`, `NEO_ROOT` 환경변수 자동 설정.
검증: `echo $HARNESS_ROOT` — 비어있으면 bootstrap이 실패한 것이다.
이 파일의 모든 `harness/` 경로는 `$HARNESS_ROOT + 경로`로 해석한다.
예: `harness/skills/gate.md` → `$HARNESS_ROOT/skills/gate.md`

## 워크플로우 게이트 (절대 건너뛰지 않는다)

- Task Brief 전달 전 반드시 harness/skills/gate.md 실행 (Q1~Q7 체크)
- Task Brief 없이 구현을 시작하지 않는다
- 새 기능 구현 전 반드시 harness/skills/phase0.md 실행 (Phase 0)

## Q1~Q7 게이트 체크리스트

Task Brief 전달 전, 아래 중 하나라도 \"예\"이면 AC(architect.md)로 전환한다:

1. 새 외부 시스템·라이브러리·서비스가 추가되는가?
2. DB 스키마가 변경되는가?
3. 기존 API의 인터페이스가 변경되는가?
4. 두 개 이상의 도메인에 영향을 주는가?
5. 비가역적 작업인가? (마이그레이션·삭제·외부 발송·결제)
6. 성능·비용·보안에 직접 영향을 주는가?
7. AI 생성 코드 보안 취약점 스캔이 필요한가?

## Omission Constraints (절대 금지 — 이유가 있어도 예외 없음)

# 아래 항목을 이 프로젝트에 맞게 작성한다.
# 예시 형식:
#
# - `{핵심 필드/컬럼}`을 {금지 행동}하지 않는다
#   이유: {왜 금지인가. 위반 시 어떤 피해가 발생하는가}
#
# - {금지 행동}하지 않는다
#   이유: {왜 금지인가}

- {절대 금지 항목 1}
  이유: {이유 1}

- {절대 금지 항목 2}
  이유: {이유 2}

- {절대 금지 항목 3}
  이유: {이유 3}

## 공통 보안 원칙 (모든 프로젝트 공통)

- Alembic (또는 프로젝트 마이그레이션 도구) 없이 DB 스키마를 직접 변경하지 않는다
  이유: 스키마 변경 추적 불가. 운영 DB 파괴 위험.
- JWT 검증을 skip하는 코드를 main/develop 브랜치에 포함하지 않는다
  이유: 인증 우회. 보안 취약점.
- SECRET 키를 코드에 하드코딩하지 않는다
  이유: 버전 관리 시스템에 노출. 보안 사고 위험.

## 참조 문서 복원 순서 (AGENTS.md 압축 시)

AGENTS.md가 컨텍스트 압축으로 소실되면 아래 순서로 복원한다:
1. `harness/AGENTS.md` 재로드
2. `harness/personas/orchestrator.md` 로드
3. `harness/personas/{architect|backend|frontend}.md` 로드
